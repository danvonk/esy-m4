{
  "name": "esy-m4",
  "version": "1.4.18",
  "description": "m4 packaged for esy",
  "esy": {
    "buildsInSource": true,
    "build": [
      "./bootstrap",
      "./configure --prefix=#{self.install}",
      "make -j8"
    ],
    "install": "make install"
  }
}
